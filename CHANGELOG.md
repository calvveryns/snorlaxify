1. В контракт pipeline/result/resolve добавлены `item_one_id` и `item_two_id`, а merge переведён на работу по `items.id` вместо сравнения по имени.
2. `vector_data` переведена на хранение `item_id`, pipeline теперь векторизует записи с привязкой к идентификатору записи.
3. После `resolve` обработанные пары удаляются из сохранённого `pipeline_results`, поэтому повторное открытие страницы дублей показывает только необработанный остаток.
4. Реализована новая логика merge: одна запись остаётся в `items`, получает имя от пользователя, вторая переносится в `items_archive` и физически удаляется из `items`.
5. Перед удалением дубля backend автоматически переносит single-column foreign key ссылки с удаляемого `items.id` на сохраняемый `items.id`.
6. Обновление имени записи исправлено: теперь backend обновляет `name.ru` и `name.en`, а не только `name.en`.
7. В векторизацию добавлены дополнительные поля записи: `path`, `typeId`, `typeIdentifier`; во внешний embedding API отправляется составной текст, а не только имя.
8. Исправлена ошибка `IndexError: tuple index out of range` в `resolve`, вызванная неверным числом SQL-параметров при обновлении `items.name`.
9. Добавлена жёсткая валидация ответа LLM: backend принимает только те пары, которые точно совпадают с входным candidate list.
10. Добавлены и обновлены тесты: контрактные тесты под новый ID-based контракт и unit test на валидацию ответа recommender.
